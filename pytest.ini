[pytest]
testpaths = tests
pythonpath = src
addopts = -v
# With coverage (requires: pip install pytest-cov; some envs need PYTHONPATH=src):
# addopts = -v --cov=json_sort --cov-report=term-missing --cov-fail-under=90
